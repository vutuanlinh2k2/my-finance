# Supabase Configuration
# For local development, run `pnpm db:start` which outputs these values
# For production, use values from your Supabase dashboard

# Local Development
# Run `pnpm db:start` and copy the anon key from the output
VITE_SUPABASE_URL=http://localhost:64321
VITE_SUPABASE_ANON_KEY=your-local-anon-key-from-supabase-start

# Production (uncomment and fill in for production builds)
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-production-anon-key

# Server-side only (not exposed to client)
# Get this from `pnpm db:start` output or Supabase dashboard
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
